selector_to_html = {"a[href=\"#rand\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">rand()<a class=\"headerlink\" href=\"#rand\" title=\"Permalink to this heading\">#</a></h2><p>An older implementation of a random number generator in C++ is the rand() function. rand() is not thread-safe, which is a modern problem - older computers had only one core.</p><p>A call to rand() returns a pseudo-random integer value on [0 and RAND_MAX] - repeated calls generate the next number in the sequence generated by the algorithm.</p>", "a[href=\"#distributions\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Distributions<a class=\"headerlink\" href=\"#distributions\" title=\"Permalink to this heading\">#</a></h2><p>A random number distribution is a function object that, when called with a random number generator argument, produces a sequence of values of its result type.</p>", "a[href=\"#normal-distribution\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Normal Distribution<a class=\"headerlink\" href=\"#normal-distribution\" title=\"Permalink to this heading\">#</a></h2><p>with mean value <span class=\"math notranslate nohighlight\">\\(\\\\mu\\)</span> and standard deviation <span class=\"math notranslate nohighlight\">\\(\\\\sigma\\)</span>. If <span class=\"math notranslate nohighlight\">\\(\\\\mu=0\\)</span> and <span class=\"math notranslate nohighlight\">\\(\\\\sigma=1\\)</span>, it is normally called the <strong>standard normal distribution</strong></p><p>For a normal distribution:\ndouble mean = 50;\ndouble standardDeviation = 10;\nstd::random_device myEngine;\nstd::normal_distribution<double> myNormalDistribution{ mean, standardDeviation };</double></p>", "a[href=\"#random-numbers\"]": "<h1 class=\"tippy-header\" style=\"margin-top: 0;\">Random Numbers<a class=\"headerlink\" href=\"#random-numbers\" title=\"Permalink to this heading\">#</a></h1><p>Many physical phenomena are random e.g. radioactive decay, and others are most easily modelled by treating them as random e.g. Brownian motion - computational physics therefore requires random numbers. It is also often useful to test a program using random numbers - and random numbers can be used to generate distributions. Randomness is the real or apparent absence of any definite patterns or predictability in data/information. The property of randomness is most often used in statistics to signify well-defined statistical properties. Monte Carlo methods rely on random inputs i.e. from a random number generator.</p><p>There is a difference between true randomness and statistical randomness - in both cases the next number cannot be predicted - but in statistical randomness there may be a probability distribution of outcomes i.e. some numbers are more likely to occur than others.</p>", "a[href=\"#srand\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">srand()<a class=\"headerlink\" href=\"#srand\" title=\"Permalink to this heading\">#</a></h2><p>Seeds the pseudo-random number generator used by rand() with the value seed, which must be an integer.\nDefined in standard library as:</p>"}
skip_classes = ["headerlink", "sd-stretched-link"]

window.onload = function () {
    for (const [select, tip_html] of Object.entries(selector_to_html)) {
        const links = document.querySelectorAll(` ${select}`);
        for (const link of links) {
            if (skip_classes.some(c => link.classList.contains(c))) {
                continue;
            }

            tippy(link, {
                content: tip_html,
                allowHTML: true,
                arrow: true,
                placement: 'auto-start', maxWidth: 500, interactive: false, theme: 'material', duration: [200, 100], delay: [200, 0],

            });
        };
    };
    console.log("tippy tips loaded!");
};
